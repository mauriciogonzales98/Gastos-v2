import { describe, expect, it } from 'vitest'

// Smoke test del andamiaje: confirma que Vitest corre.
// Se reemplaza por tests reales al arrancar la feature 1.
describe('andamiaje', () => {
  it('corre los tests', () => {
    expect(true).toBe(true)
  })
})
